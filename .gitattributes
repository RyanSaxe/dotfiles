# Normalize line endings on checkin; the repo is LF everywhere.
* text=auto eol=lf

# Keep generated-looking assets out of language statistics.
**/generated/** linguist-generated=true

# Per-machine notch compensation (see aerospace/configure.sh).
aerospace/aerospace.toml filter=aerospace-notch
