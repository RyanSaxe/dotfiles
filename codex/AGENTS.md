# Global Codex Instructions

Keep this file limited to reusable cross-machine guidance.

Use repo-local `AGENTS.md` files and project-level `.codex/config.toml` files for repository-specific behavior.
