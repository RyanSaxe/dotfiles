# Mode-pinned render exceptions. Templates not listed here render in the
# current mode; these render in the named mode on every pass, because
# ghostty's native macOS-appearance switching reads its light: and dark:
# theme files simultaneously — both must always exist.
dotfiles-dark=dark
dotfiles-light=light
