# The global default mascot — shareable, so it lives in the repo.
# Project-specific mappings NEVER go here: they contain project names and
# live in machine state (~/.local/state/dotfiles/mascot.conf) instead.
# Set with: theme mascot default <provider:id>
default=pokemon:raikou
