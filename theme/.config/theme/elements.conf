# Rendered output -> palette context. Mixed templates use explicit
# {{inner_*}} / {{outer_*}} placeholders for the other context.
cursor_trail.glsl=inner
frame.glsl=outer
shell-colors.zsh=inner
sketchybar-colors.sh=outer
starship.toml=inner
tmux-colors.conf=mixed
tuis-colors.json=outer
