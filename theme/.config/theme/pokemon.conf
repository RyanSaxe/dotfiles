# The global default pokemon — shareable, so it lives in the repo.
# Project-specific mappings NEVER go here: they contain project names and
# live in machine state (~/.local/state/dotfiles/pokemon.conf) instead.
# Set with: theme pokemon default <name> [--shiny]
default=raikou
