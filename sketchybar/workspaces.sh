#!/usr/bin/env zsh
# The aerospace workspaces the bar shows, left to right — the monitor order.
# Sourced by sketchybarrc to build the items and by plugins/aerospace.sh to
# repaint them, so the list exists once and the two cannot disagree.
WORKSPACES=(message browser code meeting)
