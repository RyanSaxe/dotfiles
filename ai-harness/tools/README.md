# Tools

This directory is the canonical root for self-managed CLIs intended for agents
across projects. Activation links each reviewed executable individually into
`~/.local/bin`; it never replaces that directory.

No v1 tools have been migrated. Review them individually after the harness
scaffold is approved.
