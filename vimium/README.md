# Vimium

Vimium is a browser extension to let you use a variety of vim navigation tricks in the browser. To use this, just do the following:

1. Install the extension in the browser of your choice (NOTE: this repo is only tested on Chrome)
2. Upload the ./vimium-options.json in the restore area of the settings
