{
  "name": "rail",
  "private": true,
  "type": "module",
  "description": "In-terminal sidebar: tmux windows + agent status, themed by the dotfiles theme system",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
