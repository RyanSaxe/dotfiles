return {
  "folke/flash.nvim",
  opts = {
    -- these settings just let you have typos without canceling the search
    search = {
      mode = "search",
    },
    modes = { char = { enabled = true } },
  },
}
