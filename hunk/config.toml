# Hunk 0.10.0 supports built-in themes only. Use "midnight" until custom
# TokyoNight theme support lands in a published hunkdiff release.
theme = "midnight"
mode = "auto"
exclude_untracked = true
line_numbers = true
wrap_lines = false
agent_notes = true
